import React, { useState } from 'react';

import Flashcard from 'components/Flashcard';

// import Button from '@material-ui/core/Button';

export default function FlashcardsTest(props) {
  const { cards } = props;

  return (
    <>
      <Flashcard />
    </>
  );
}
